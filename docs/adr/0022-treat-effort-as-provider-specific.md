---
status: accepted
---

# Treat effort as provider-specific

effortまたはreasoning設定をprovider間で共通の尺度へ正規化しない。各Model Profileはprovider固有の設定値をそのまま固定し、各model/roleでprovider defaultと隣接する低・高設定をDevelopment Cohort上で比較する。effortを公開しないmodelは固定値として扱い、採用判断は設定名ではなく実測cost、latency、品質に基づける。
